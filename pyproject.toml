[build-system]
requires = [
  "setuptools>=64",
  "wheel",
  "torch",
]
build-backend = "setuptools.build_meta"

[project]
name = "simple_knn"
version = "1.0.0"
description = "CUDA extension for simple k-nearest neighbor queries"
requires-python = ">=3.8"
